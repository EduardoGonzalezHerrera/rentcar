spring:
  application:
    name: customer
server:
  port: 8080
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    instance-id: ${spring.application.name}-${random.int}    